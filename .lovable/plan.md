Update the Reports Configuration card visuals:

1. Switch toggle grid from 2 columns to 3 columns on medium+ screens (`md:grid-cols-2` → `md:grid-cols-3`).
2. Add a green left accent border to the Report Type and Scope section containers (e.g., `border-l-4 border-l-green-500` or via the project's primary success/green token).

No functional changes to save/load behavior or visibility logic.