Apply the same green left accent border to every group in the Reports Configuration card (Filters and Action Buttons, in addition to the existing Report Type and Scope). No other changes to layout or behavior.